name: CI/CD Demo

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - 🐙 name: Checkout code
        uses: actions/checkout@v4

      - 🧪 name: Run test
        run: echo "Test passed"

      - 🚀 name: Deploy
        run: echo "Deploy successful"
